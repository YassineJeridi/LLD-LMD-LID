# TASK 2 — Core Website Structure (HTML + CSS)

- Build `index.html` with a sidebar navigation (LDD / LMD / LID), a main content area, and a top header
- Build `style.css` with a modern, clean design: dark/light mode toggle, syntax-highlighted code blocks, responsive layout, smooth transitions
- Sidebar shows all three categories and expands subtopics on click
- Active state highlights the current section being viewed
- Code blocks are styled distinctly (monospace, colored background)
