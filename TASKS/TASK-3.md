# TASK 3 — JavaScript Logic + Full Interactivity

- Build `main.js` to fetch all JSON files on load
- Dynamically render sections into the DOM based on the active category
- Implement sidebar navigation (clicking a topic scrolls/renders its content)
- Add a search bar that filters topics across all three files in real time
- Add a copy-to-clipboard button on each code block
- Add a progress tracker (checkboxes) so the user can mark topics as done (saved in localStorage)
- Add smooth scroll and active link highlighting as the user reads
